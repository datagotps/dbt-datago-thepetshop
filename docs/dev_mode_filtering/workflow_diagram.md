# Development Filtering Workflow

## Before (Manual Process)

```
┌─────────────────────────────────────────────────────────────┐
│  Development Cycle (OLD WAY)                                │
└─────────────────────────────────────────────────────────────┘

Step 1: Enable Dev Filtering
├─ Open dim_customers.sql → Uncomment WHERE clause
├─ Open fact_commercial.sql → Uncomment WHERE clause  
├─ Open fact_orders.sql → Uncomment WHERE clause
├─ Open fct_daily_transactions.sql → Uncomment WHERE clause
└─ Open fct_procurement.sql → Uncomment WHERE clause
   ⏱️  Time: 5-10 minutes

Step 2: Run Models
└─ dbt run
   ⏱️  Time: Faster (filtered data)

Step 3: Disable Dev Filtering (Before Production)
├─ Open dim_customers.sql → Comment WHERE clause
├─ Open fact_commercial.sql → Comment WHERE clause
├─ Open fact_orders.sql → Comment WHERE clause
├─ Open fct_daily_transactions.sql → Comment WHERE clause
└─ Open fct_procurement.sql → Comment WHERE clause
   ⏱️  Time: 5-10 minutes
   ⚠️  Risk: Forgetting to comment = filtered production data!

Step 4: Production Run
└─ dbt run
   ⏱️  Time: Full processing time

Total Manual Edits per Cycle: 10 files
Risk Level: HIGH (manual errors)
```

## After (Automated Process)

```
┌─────────────────────────────────────────────────────────────┐
│  Development Cycle (NEW WAY)                                │
└─────────────────────────────────────────────────────────────┘

Step 1: Development Run (Filtered Data)
└─ dbt run --vars 'dev_mode: true'
   ⏱️  Time: <1 second to enable
   ✅ All 5 models automatically filtered

Step 2: Production Run (Full Data)
└─ dbt run
   ⏱️  Time: <1 second (default behavior)
   ✅ All 5 models automatically use full data

Total Manual Edits per Cycle: 0 files
Risk Level: ZERO (no manual editing)
```

## Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    dbt_project.yml                           │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ vars:                                                  │  │
│  │   dev_mode: false          ← Default (Production)     │  │
│  │   dev_date_ranges:         ← Centralized Config      │  │
│  │     - {start: '2025-01-01', end: '2025-09-30'}       │  │
│  │     - {start: '2024-12-01', end: '2024-12-31'}       │  │
│  │     - {start: '2024-01-01', end: '2024-01-31'}       │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│              macros/dev_date_filter.sql                      │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ {% macro dev_date_filter(date_column, ranges) %}      │  │
│  │   {% if var('dev_mode', false) %}                     │  │
│  │     AND (                                             │  │
│  │       date_column BETWEEN 'start' AND 'end'          │  │
│  │       OR ...                                          │  │
│  │     )                                                 │  │
│  │   {% endif %}                                         │  │
│  │ {% endmacro %}                                        │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                    Your Models                               │
│  ┌─────────────────┬──────────────────┬──────────────────┐  │
│  │ dim_customers   │ fact_commercial  │ fact_orders      │  │
│  │                 │                  │                  │  │
│  │ WHERE ...       │ WHERE ...        │ WHERE ...        │  │
│  │ {{ dev_date_    │ {{ dev_date_     │ {{ dev_date_     │  │
│  │    filter(...)}}│    filter(...)}} │    filter(...)}} │  │
│  └─────────────────┴──────────────────┴──────────────────┘  │
│  ┌─────────────────┬──────────────────────────────────────┐  │
│  │ fct_daily_trans │ fct_procurement                      │  │
│  │                 │                                      │  │
│  │ WHERE ...       │ WHERE ...                            │  │
│  │ {{ dev_date_    │ {{ dev_date_filter(..., custom)}}   │  │
│  │    filter(...)}}│                                      │  │
│  └─────────────────┴──────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## Command Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Command: dbt run --vars 'dev_mode: true'                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ dbt reads     │
                    │ dev_mode=true │
                    └───────────────┘
                            │
                            ▼
        ┌───────────────────┴───────────────────┐
        │                                       │
        ▼                                       ▼
┌───────────────┐                      ┌───────────────┐
│ Macro checks  │                      │ Macro checks  │
│ var('dev_mode')│                     │ var('dev_mode')│
│ = true        │                      │ = true        │
└───────────────┘                      └───────────────┘
        │                                       │
        ▼                                       ▼
┌───────────────┐                      ┌───────────────┐
│ Adds date     │                      │ Adds date     │
│ filter to SQL │                      │ filter to SQL │
└───────────────┘                      └───────────────┘
        │                                       │
        └───────────────────┬───────────────────┘
                            ▼
                    ┌───────────────┐
                    │ Compiled SQL  │
                    │ with filters  │
                    └───────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ Execute on    │
                    │ filtered data │
                    └───────────────┘
```

```
┌─────────────────────────────────────────────────────────────┐
│  Command: dbt run  (default)                                │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ dbt reads     │
                    │ dev_mode=false│
                    └───────────────┘
                            │
                            ▼
        ┌───────────────────┴───────────────────┐
        │                                       │
        ▼                                       ▼
┌───────────────┐                      ┌───────────────┐
│ Macro checks  │                      │ Macro checks  │
│ var('dev_mode')│                     │ var('dev_mode')│
│ = false       │                      │ = false       │
└───────────────┘                      └───────────────┘
        │                                       │
        ▼                                       ▼
┌───────────────┐                      ┌───────────────┐
│ No date filter│                      │ No date filter│
│ added         │                      │ added         │
└───────────────┘                      └───────────────┘
        │                                       │
        └───────────────────┬───────────────────┘
                            ▼
                    ┌───────────────┐
                    │ Compiled SQL  │
                    │ without filter│
                    └───────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ Execute on    │
                    │ full data     │
                    └───────────────┘
```

## Data Flow Comparison

```
┌─────────────────────────────────────────────────────────────┐
│  Development Mode (dev_mode: true)                          │
└─────────────────────────────────────────────────────────────┘

Source Tables          Filtered Data         Final Models
┌──────────┐          ┌──────────┐          ┌──────────┐
│ All Data │          │ Jan 2024 │          │ dim_     │
│ 2020-2025│  ──────> │ Dec 2024 │  ──────> │ customers│
│ (5 years)│          │ Jan-Sep  │          │          │
└──────────┘          │ 2025     │          └──────────┘
                      └──────────┘
                      ⚡ Fast                 ⚡ Quick
                      💰 Cheap                  Testing
```

```
┌─────────────────────────────────────────────────────────────┐
│  Production Mode (dev_mode: false - default)                │
└─────────────────────────────────────────────────────────────┘

Source Tables          All Data              Final Models
┌──────────┐          ┌──────────┐          ┌──────────┐
│ All Data │          │ All Data │          │ dim_     │
│ 2020-2025│  ──────> │ 2020-2025│  ──────> │ customers│
│ (5 years)│          │ (5 years)│          │          │
└──────────┘          └──────────┘          └──────────┘
                      🎯 Complete            ✅ Production
                      📊 Accurate               Ready
```

## Benefits Visualization

```
┌─────────────────────────────────────────────────────────────┐
│                    Time Savings                             │
└─────────────────────────────────────────────────────────────┘

Old Way (Manual):
Development Cycle: [████████████████████] 20 minutes
├─ Manual edits: 10 min
├─ dbt run: 8 min
└─ Undo edits: 2 min

New Way (Automated):
Development Cycle: [████] 8 minutes
└─ dbt run --vars 'dev_mode: true': 8 min

Savings: 60% faster per cycle
```

```
┌─────────────────────────────────────────────────────────────┐
│                    Risk Reduction                           │
└─────────────────────────────────────────────────────────────┘

Old Way:
Risk of Production Issues: [████████] HIGH
├─ Forgot to uncomment: 40%
├─ Wrong date ranges: 30%
└─ Inconsistent filters: 30%

New Way:
Risk of Production Issues: [█] MINIMAL
└─ Default is production mode: Safe by design
```
